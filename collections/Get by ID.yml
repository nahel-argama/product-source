info:
  name: Get by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/products/:product-id"
  params:
    - name: product-id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
